name: Azure Container Deployment Pipeline

on: [push]

env:
  DOCKER_IMAGE: ${{ secrets.AZURE_USERNAME }}/webapp:${{ github.sha }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t $DOCKER_IMAGE ./app

      # FIXED DOCKER LOGIN
      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.AZURE_USERNAME }}" --password-stdin

      - name: Push Docker image
        run: docker push $DOCKER_IMAGE

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Write SSH Keys
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/azure-container-key
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > ~/.ssh/azure-container-key.pub
          chmod 600 ~/.ssh/azure-container-key

      - name: Azure Login
        run: |
          az login -u "${{ secrets.AZURE_USERNAME }}" -p "${{ secrets.AZURE_PASSWORD }}"
          az account show

      - name: Terraform Init
        run: terraform init ./terraform

      - name: Terraform Apply
        run: terraform apply -auto-approve ./terraform
        env:
          TF_VAR_admin_username: "${{ secrets.AZURE_USERNAME }}"
          TF_VAR_ssh_public_key: "${{ secrets.SSH_PUBLIC_KEY }}"

      - name: Get Public IP
        run: |
          IP=$(terraform -chdir=./terraform output -raw public_ip)
          echo "PUBLIC_IP=$IP" >> $GITHUB_ENV

      - name: Setup Ansible
        run: sudo apt update && sudo apt install -y ansible

      - name: Run Ansible Playbook
        run: |
          ansible-playbook \
            -i "${{ env.PUBLIC_IP }}," \
            -u "${{ secrets.AZURE_USERNAME }}" \
            --private-key ~/.ssh/azure-container-key \
            -e "docker_image=$DOCKER_IMAGE" \
            ansible/docker.yml